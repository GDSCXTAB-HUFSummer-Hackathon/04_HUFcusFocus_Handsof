import { Container } from "components/common/shared";
import { MainContent } from "components/main";

const Main = () => {
  return (
    <Container>
      <MainContent />
    </Container>
  );
};

export default Main;
